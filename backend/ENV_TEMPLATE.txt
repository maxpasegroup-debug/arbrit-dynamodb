# AWS DynamoDB Configuration
# Replace with your AWS credentials
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# JWT Secret Key
# Generate a secure random key for production
JWT_SECRET_KEY=your-secret-key-change-in-production

# CORS Origins
# Comma-separated list of allowed origins
CORS_ORIGINS=*

# IMPORTANT NOTES:
# 1. Use your ACTUAL AWS credentials with DynamoDB access
# 2. All data is stored in DynamoDB tables (arbrit-*)
# 3. Ensure IAM role has DynamoDB read/write permissions
# 4. Change JWT secret key in production
# 5. Update CORS_ORIGINS with your actual domain(s)
