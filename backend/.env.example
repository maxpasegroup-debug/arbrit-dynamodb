# MongoDB Configuration
MONGO_URL=mongodb+srv://USERNAME:PASSWORD@your-cluster.mongodb.net/arbrit-workdesk?retryWrites=true&w=majority
DB_NAME=arbrit-workdesk

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-to-random-string

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=https://www.happinotes.in,https://happinotes.in,http://localhost:3000

# Optional: Environment identifier
ENV=production
